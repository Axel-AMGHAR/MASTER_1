/*
import { createClient } from './node_modules/pexels';
*/

const {createClient} = require('pexels')
cl(createClient)

/*const cl = e => console.log(e);
cl('test')
const client = createClient('563492ad6f91700001000001a089d72b88ba4393acaa7d770b916f17');
const query = 'Nature';

client.photos.search({ query, per_page: 1 }).then(photos => cl(photos));*/
